{
	"name": "user_vk_bot",
	"version": "1.0.0",
	"main": "index.js",
	"license": "MIT",
	"repository": "https://github.com/RusAnonym/user-vk-bot",
	"scripts": {
		"test": "yarn build && cd out && node test.js",
		"start": "yarn build && cd out && node main.js",
		"build": "tsc"
	},
	"dependencies": {
		"moment": "^2.29.1",
		"rus-anonym-utils": "^1.0.12",
		"simple-scheduler-task": "^3.0.12",
		"vk-io": "^4.0.3"
	},
	"devDependencies": {
		"@types/node": "^14.14.2"
	}
}
